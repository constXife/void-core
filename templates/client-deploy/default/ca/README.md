# CA

This directory is reserved for client-owned trust bootstrap material.

Expected contents for the private CA pilot path:
- `root-ca.crt` — root certificate installed into the local trust store;
- optional exported bootstrap files for client devices.

This directory intentionally ships without a real certificate.
