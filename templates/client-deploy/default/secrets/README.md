# Secrets

This directory is intentionally a placeholder.

Clients are expected to keep encrypted secret material under their own control.

The example file `core-01.yaml.example` only documents the expected shape.

The first-wave shape currently covers:
- `rauthy.env` for the auth baseline;
- optional `step-ca.json` for the private CA pilot path.
