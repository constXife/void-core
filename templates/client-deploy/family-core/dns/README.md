# DNS

This directory is reserved for client-owned private zone material.

The example file `site.zone.example` is intentionally narrow:
- one site domain;
- one host;
- `id.<site-domain>`;
- `ca.<site-domain>`.

Replace it with a real zone file before enabling the CoreDNS baseline.
