# Auth Modules

This directory contains foundation-level authentication modules.

The first scaffolded auth module is:
- `rauthy.nix` — a minimal Rauthy integration based on:
  - file-delivered secrets,
  - a shared naming contract,
  - a generated config template,
  - a podman-backed runtime.

This is intentionally a narrow first-wave baseline, not a final auth platform.
